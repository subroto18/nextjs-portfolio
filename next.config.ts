import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "trydo.rainbowit.net",
      "cdn.pixabay.com",
      "images.indianexpress.com",
      "scontent.fshl2-1.fna.fbcdn.net",
    ],
  },
};

export default nextConfig;
